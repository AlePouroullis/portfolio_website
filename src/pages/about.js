import * as React from 'react';
import Layout from '../components/layout';

const AboutPage = () => {
    return (
        <Layout pageTitle="About Me">
            <p>This is the children prop for the "About Me" page.</p>
        </Layout>
    );
}

export default AboutPage;